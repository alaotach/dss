files:
  "/etc/nginx/conf.d/proxy.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      client_max_body_size 20M;
      proxy_read_timeout 300;
      proxy_connect_timeout 300;
      proxy_send_timeout 300;

container_commands:
  01_migrate:
    command: "python -c 'from repositories.database import init_db; init_db()'"
    leader_only: true
